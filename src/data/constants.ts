// Re-exported from content collection: src/content/siteContent/ui-config.json
import uiConfigData from '../content/siteContent/ui-config.json';

const ui = uiConfigData.uiConfig;

// Berita category colors - used in agenda, berita pages
export const kategoriBeritaColors = ui.kategoriBeritaColors;

// Berita timeline colors - used in home page timeline
export const kategoriBeritaTimelineColors = ui.kategoriBeritaTimelineColors;

// Time of day colors - used in agenda
export const waktuColors = ui.waktuColors;

// Wisata facilities icons
export const fasilitasIcons = ui.fasilitasIcons;

// UMKM category icons
export const umkmKategoriIcons = ui.umkmKategoriIcons;

// UMKM category labels with colors
export const umkmKategoriLabels = ui.umkmKategoriLabels;

// Galeri category list
export const galeriKategoriList = ui.galeriKategoriList;

// Galeri category labels
export const galeriKategoriLabels = ui.galeriKategoriLabels;
