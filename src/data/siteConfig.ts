// Re-exported from content collection: src/content/siteContent/site-config.json
// This file provides synchronous access for components that can't use async getEntry()
import siteConfigData from '../content/siteContent/site-config.json';

export const siteConfig = siteConfigData.siteConfig;
