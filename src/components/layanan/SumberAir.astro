---
import { getEntry } from 'astro:content';

const data = await getEntry('layanan', 'layanan');
const sumberAir = data?.data?.sumberAir || [];
---

<div class="bg-white rounded-2xl p-6 md:p-8 shadow-lg" data-aos="fade-up">
  <div class="flex items-center gap-3 mb-6">
    <div
      class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center"
    >
      <span class="text-2xl">💧</span>
    </div>
    <h2 class="text-2xl font-bold text-gray-900">Sumber Air</h2>
  </div>
  <p class="text-gray-600 mb-6">
    Dusun Bedalo mendapatkan suplai air dari berbagai sumber:
  </p>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    {
      sumberAir.map((item, index) => (
        <div
          class="bg-blue-50 rounded-xl p-4 flex items-start gap-3"
          data-aos="fade-up"
          data-aos-delay={Math.min(index * 100, 300)}
        >
          <span class="text-2xl">{item.icon}</span>
          <span class="text-gray-700">{item.text}</span>
        </div>
      ))
    }
  </div>
</div>
