---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import { siteConfig } from '../../data/siteConfig';
import { uiStrings } from '../../data/uiStrings';

interface Props {
  kode: string;
  kampus: string;
  kelompok: string;
  tema: string;
  periodeMulai: string;
  periodeSelesai: string;
  heroImage?: ImageMetadata;
}

const {
  kode,
  kampus,
  kelompok,
  tema,
  periodeMulai,
  periodeSelesai,
  heroImage,
} = Astro.props;

const applyTemplate = (
  template: string,
  vars: Record<string, string | number>
) => template.replace(/\{(\w+)\}/g, (_, key) => String(vars[key] ?? ''));

const heroImageAlt = uiStrings.kkn.hero.imageAltTemplate
  ? applyTemplate(uiStrings.kkn.hero.imageAltTemplate, {
      kode,
      siteName: siteConfig.name,
    })
  : '';

function formatDate(dateStr: string): string {
  const date = new Date(dateStr);
  return date.toLocaleDateString('id-ID', {
    day: 'numeric',
    month: 'long',
    year: 'numeric',
  });
}
---

<section
  class="relative overflow-hidden bg-gradient-to-br from-primary/5 via-white to-accent/5"
>
  <div class="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-12 lg:py-20">
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <div data-aos="fade-right">
        <div
          class="inline-flex items-center gap-2 px-4 py-2 bg-primary/10 rounded-full text-primary text-sm font-medium mb-6"
        >
          <span>🎓</span>
          <span>KKN {kode}</span>
        </div>

        <h1
          class="text-4xl md:text-5xl lg:text-6xl font-extrabold text-gray-900 mb-4"
        >
          <span class="gradient-text">KKN {kode}</span>
          <br />
          <span
            class="text-2xl md:text-3xl lg:text-4xl font-bold text-gray-700"
          >
            {siteConfig.name}
          </span>
        </h1>

        <div class="space-y-3 mb-6">
          <p class="flex items-center gap-2 text-gray-600">
            <span class="text-xl">🏫</span>
            <span class="font-medium">{kampus}</span>
          </p>
          <p class="flex items-center gap-2 text-gray-600">
            <span class="text-xl">👥</span>
            <span>{kelompok}</span>
          </p>
          <p class="flex items-center gap-2 text-gray-600">
            <span class="text-xl">📅</span>
            <span>
              {formatDate(periodeMulai)} - {formatDate(periodeSelesai)}
            </span>
          </p>
        </div>

        <div
          class="bg-white rounded-xl p-4 shadow-md border-l-4 border-primary mb-8"
        >
          <p class="text-sm text-gray-500 mb-1">
            {uiStrings.kkn.hero.themeLabel}
          </p>
          <p class="text-gray-800 font-medium">{tema}</p>
        </div>

        <div class="flex flex-wrap gap-3">
          <a
            href="#program-kerja"
            class="inline-flex items-center gap-2 px-6 py-3 bg-primary text-white font-semibold rounded-xl shadow-md hover:bg-primary-hover hover:-translate-y-0.5 hover:shadow-lg transition-all"
          >
            <span>📋</span>
            <span>{uiStrings.kkn.hero.viewPrograms}</span>
          </a>
          <a
            href="#galeri"
            class="inline-flex items-center gap-2 px-6 py-3 bg-white text-gray-700 font-semibold rounded-xl shadow-md hover:bg-gray-50 hover:-translate-y-0.5 hover:shadow-lg transition-all border border-gray-200"
          >
            <span>📷</span>
            <span>{uiStrings.kkn.hero.viewGallery}</span>
          </a>
        </div>
      </div>

      <div class="relative" data-aos="fade-left">
        {
          heroImage ? (
            <div class="relative rounded-2xl overflow-hidden shadow-2xl">
              <Image
                src={heroImage}
                alt={heroImageAlt}
                width={800}
                height={600}
                class="w-full h-auto object-cover"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/30 to-transparent" />
            </div>
          ) : (
            <div class="relative rounded-2xl overflow-hidden shadow-2xl bg-gradient-to-br from-primary/20 to-accent/20 aspect-[4/3] flex items-center justify-center">
              <span class="text-8xl">🏘️</span>
            </div>
          )
        }
        <div
          class="absolute -bottom-4 -right-4 w-24 h-24 bg-accent/20 rounded-full blur-2xl"
        >
        </div>
        <div
          class="absolute -top-4 -left-4 w-32 h-32 bg-primary/20 rounded-full blur-2xl"
        >
        </div>
      </div>
    </div>
  </div>
</section>
