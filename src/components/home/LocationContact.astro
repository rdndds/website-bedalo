---
import { siteConfig } from '../../data/siteConfig';
import { getEntry } from 'astro:content';
import Button from '../common/Button.astro';

const lokasiData = await getEntry('profil', 'lokasi');
const lokasi = lokasiData?.data?.lokasi;

const sectionData = await getEntry('siteContent', 'section-titles');
const sectionTitles = sectionData?.data?.sectionTitles;

const distances = lokasi?.jarakFasilitas?.slice(0, 3) || [];
---

<section class="relative py-16 lg:py-24 overflow-hidden bg-secondary">
  <div class="absolute inset-0">
    <iframe
      src={siteConfig.mapsEmbedUrl}
      class="w-full h-full grayscale opacity-30"
      style="border:0;"
      allowfullscreen
      loading="lazy"
      referrerpolicy="no-referrer-when-downgrade"
      title="Lokasi Dusun Bedalo"
    >
    </iframe>
    <div
      class="absolute inset-0 bg-gradient-to-r from-primary/90 via-primary/70 to-primary/90"
    >
    </div>
  </div>

  <div class="relative max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <div data-aos="fade-right">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          {sectionTitles?.location?.title || 'Kunjungi Kami'}
        </h2>
        <p class="text-blue-100 mb-8 text-lg">
          {
            sectionTitles?.location?.subtitle ||
              'Dusun Bedalo terletak di pesisir selatan Gunungkidul, berbatasan langsung dengan Samudra Hindia.'
          }
        </p>

        <div class="flex flex-wrap gap-3 mb-8">
          {
            distances.map((d) => (
              <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2 flex items-center gap-2">
                <span class="text-lg">{d.icon}</span>
                <span class="text-white text-sm">
                  <strong>
                    {d.jarak} {d.satuan}
                  </strong>{' '}
                  ke {d.nama}
                </span>
              </div>
            ))
          }
        </div>

        <div class="flex flex-wrap gap-4">
          <Button
            href={siteConfig.mapsUrl}
            variant="secondary"
            size="lg"
            class="gap-2 rounded-xl shadow-lg"
          >
            <svg
              class="w-5 h-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
              >
              </path>
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"
              >
              </path>
            </svg>
            Buka di Google Maps
          </Button>
          <Button
            href="/kontak"
            variant="outline"
            size="lg"
            class="gap-2 rounded-xl border-white text-white hover:bg-white/10 hover:text-white"
          >
            <svg
              class="w-5 h-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
              >
              </path>
            </svg>
            Hubungi Kami
          </Button>
        </div>
      </div>

      <div
        class="lg:justify-self-end"
        data-aos="fade-left"
        data-aos-delay="100"
      >
        <div class="glass-card p-6 md:p-8 rounded-3xl max-w-md">
          <h3 class="text-white font-bold text-xl mb-6 flex items-center gap-2">
            <span class="text-2xl">📍</span>
            Alamat Lengkap
          </h3>

          <address class="not-italic text-blue-100 space-y-2 mb-6">
            <p class="font-semibold text-white text-lg">
              Dusun {siteConfig.address.dusun}
            </p>
            <p>Kalurahan {siteConfig.address.kalurahan}</p>
            <p>Kapanewon {siteConfig.address.kapanewon}</p>
            <p>Kabupaten {siteConfig.address.kabupaten}</p>
            <p>{siteConfig.address.provinsi}</p>
            <p class="text-accent font-medium">{siteConfig.address.kodePos}</p>
          </address>

          <div class="pt-4 border-t border-white/20">
            <p class="text-blue-100 text-sm mb-3">Kontak Cepat:</p>
            <div class="flex flex-wrap gap-3">
              <a
                href="https://wa.me/6281234567890"
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-2 px-4 py-2 bg-green-500 text-white text-sm font-medium rounded-lg hover:bg-green-600 transition-colors"
              >
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                  <path
                    d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"
                  >
                  </path>
                </svg>
                WhatsApp
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .glass-card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
</style>
